<view class="card">
    <view class="info1">金秋艺术节 | {{data.title}}</view>
    <view class="info3">
        <view class="info"><view class="num">{{data.other.region}}</view><view class="text">区域</view></view>
        <view class="info"><view class="num">{{info.count}}</view><view class="text">已抢票数</view></view>
        <view class="info"><view class="num">{{data.count}}</view><view class="text">总票数</view></view>
    </view>
    <button class="btn" loading="{{loading}}" bind:tap="ticket">
        抢
    </button>
    
</view>

